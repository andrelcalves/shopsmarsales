# Backend API URL (used at build time by Create React App)
# For local development, leave empty to use default http://localhost:4000
# For production, set to the ALB/backend URL
REACT_APP_API_URL=http://localhost:4000
